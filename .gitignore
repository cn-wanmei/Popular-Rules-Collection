__pycache__/
*.pyc
.venv/
# Legacy monolithic provenance — never commit (GitHub 100MB limit)
database/provenance/domains.jsonl
# V2.1 local/CI release pack carrier
dist/
# Content-Addressed Storage objects — binary, unbounded size, never commit
data/cas/
# Immutable IR snapshot can exceed GitHub's 100MB per-file limit; keep it in CI artifacts
# and retain the smaller run manifests/canonical data in the repository.
data/runs/*/ir/ir.json
