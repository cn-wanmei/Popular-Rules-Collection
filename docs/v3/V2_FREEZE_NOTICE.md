# V2 Freeze Notice

Effective with `reports/baseline/V2_FREEZE.json`:

- **V2 code path** (`scripts/`, Collect workflow): production + oracle.
- **No new architecture features** on V2 except critical/security/upstream emergency.
- **V3 work** only under `src/v3/`, `config/v3/`, `data/v3/`, `tests/v3/`.
- Broken half-migration shims to missing `src.hierarchy.*` restored to self-contained V2 scripts.
