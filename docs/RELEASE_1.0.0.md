# 1.0.0 RC Announced

**Layout:** `src/engine`, `data/generated`, `generated/`, `reports/release` — no `v3/` directories.

**CLI:** `python -m src.engine.cli all`

**Cutover:** `production_cutover=true` — Collect runs engine + publish into `generated/`.
