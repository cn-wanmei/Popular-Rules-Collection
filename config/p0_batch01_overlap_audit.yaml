version: 1
schema: p0_service_overlap_audit_v1
batch: 01
purpose: Prevent unsupported claims about cross-service overlap while recording verified Batch 01 identity-level separation.
scope:
  services: [appstore, testflight, appledev, findmy]
  comparison_basis: canonical_rule_identity
status: partial
identity_level:
  appstore_testflight:
    status: verified_disjoint
    shared_rule_ids: []
  appstore_findmy:
    status: verified_disjoint
    shared_rule_ids: []
  testflight_findmy:
    status: verified_disjoint
    shared_rule_ids: []
blocked:
  - pair: [appstore, appledev]
    reason: appledev service-specific snapshot is not proven
  - pair: [testflight, appledev]
    reason: appledev service-specific snapshot is not proven
  - pair: [findmy, appledev]
    reason: appledev service-specific snapshot is not proven
semantic_overlap:
  status: pending_execution
  reason: Canonical identity disjointness does not prove runtime matcher disjointness, especially for HOST-KEYWORD rules.
next_required: execute production matcher overlap probes, then global canonical overlap evaluation after all Batch 01 snapshots and memberships are materialized
