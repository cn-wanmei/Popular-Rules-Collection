# Independent Child Service Audit

The audit boundary follows the execution plan: independent product identity + stable entry/hostname/domain family/API/control-plane boundary + routing relevance. CDN, ASN, generic cloud infrastructure and transient redirects are excluded by default.
