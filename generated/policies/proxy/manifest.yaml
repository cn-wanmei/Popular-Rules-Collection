version: 1
id: policy-proxy
description: PROXY routing composition
routing_layers:
  - geosite
includes:
  - kind: geosite
    id: geosite-proxy
    path: database/geosite/proxy.txt
    layer: geosite
notes: >
  Category-level proxy domains; Service layer outranks this set when both match.
